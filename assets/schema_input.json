{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/xiaoli-dong/covflow/main/assets/schema_input.json",
    "title": "xiaoli-dong/covflow pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.bam$",
                "errorMessage": "Bam file must be provided, cannot contain spaces and must have extension '.bam'"
            },
            "ref_fasta": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.fasta$",
                "errorMessage": "Fasta file for cannot contain spaces and must have extension '.fasta"
            },
             "bed_file": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.bed$",
                "errorMessage": "Bed file for amplicon region cannot contain spaces and must have extension 'bed"
            }
        },
        "required": ["sample", "bam"]
    }
}
